// For debugging and testing
{
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"jsx": "react-jsx",
		"isolatedModules": true // Faster transpilation (it allow not check types inter different modules)
	}
}
